<!DOCTYPE html>
<html>
  <head>
    <title>Tacostagram Final</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css">
  </head>

  <body>
    <div class="container-fluid pt-4">
      <% if @current_user %>
        <a class="btn btn-info" href="/">Home</a>
        <li class="nav-item">Logged in as: <%= @current_user["username"] %>       <a class="btn btn-outline-danger col 4" href="/logout">Logout</a> </li>
      <% else %>
        <a class="btn btn-info" href="/login">Login</a>
        <a class="btn btn-outline-info" href="/users/new">Signup</a>
      <% end %>
    </div>

    <%= yield %>
    <script scr="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/js/bootstrap.bundle.min.js"> </script>
  </body>
</html>
